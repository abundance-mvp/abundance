# Claude Project Config

This repo is **Claude Code–ready**.

- **Skills** are under `.claude/skills/` (auto-loaded).
- **Commands** are under `.claude/commands/` (user-invoked via `/command-name`).
- **Archived** items are under `.claude/archive/` (deprecated but preserved for reference).
- **settings.json** registers marketplaces and enables repo-scoped plugins.
- Trust this folder when prompted by Claude Code to enable auto-install.

## Archived Skills

The following skills have been deprecated and moved to `.claude/archive/skills/`:

- `ios-sprint-executor` - Merged into `verified-stage-development` orchestrator (2026-01-14)
